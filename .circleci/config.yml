version: 2.1

jobs:
  build-for-linux:
    docker: 
      - image: dreladen/ffxi_server

    steps:
      - checkout
      - run: pip3 install -r tools/requirements.txt
      - run: cp conf/default/* conf/
      - run: cp scripts/settings/default/* scripts/settings/
      - run: mkdir build
      - run: cd build
      - run: ls ../project
      - run: cmake ../project
      - run: make -j 4

workflows:
  version: 2.1
  build:
    jobs:
      - build-for-linux